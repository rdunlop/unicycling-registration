diff --git a/spec/lib/race_calculator_spec.rb b/spec/lib/race_calculator_spec.rb
index c8fcc27..d33a127 100644
--- a/spec/lib/race_calculator_spec.rb
+++ b/spec/lib/race_calculator_spec.rb
@@ -43,22 +43,41 @@ describe RaceCalculator do
       @tr1.place.should == "DQ"
     end
 
-    it "places fast times first" do
-      @tr1.thousands = 0
-      @tr1.seconds = 30
-      @tr1.minutes = 3
-      @tr1.save!
+    describe "when tr1 is slower than tr2" do
+      before(:each) do
+        @tr1.thousands = 0
+        @tr1.seconds = 30
+        @tr1.minutes = 3
+        @tr1.save!
 
-      # faster:
-      @tr2.thousands = 120
-      @tr2.seconds = 40
-      @tr2.minutes = 2
-      @tr2.save!
+        # faster:
+        @tr2.thousands = 120
+        @tr2.seconds = 40
+        @tr2.minutes = 2
+        @tr2.save!
+      end
+      it "places fast times first" do
 
-      @calc.update_all_places
+        @calc.update_all_places
 
-      @tr1.place.should == 2
-      @tr2.place.should == 1
+        @tr1.place.should == 2
+        @tr2.place.should == 1
+      end
+      describe "when the first competitor is ineligible" do
+        before(:each) do
+          @reg = @tr1.competitor.registrants.first
+          @reg.ineligible = true
+          @reg.save!
+          @calc.update_all_places
+        end
+
+        it "places the competitor as first" do
+          @tr1.place.should == 1
+        end
+        it "places the 2nd place as 1st also" do
+          @tr2.place.should == 2
+        end
+      end
     end
 
     describe "if 2 times are identical" do
