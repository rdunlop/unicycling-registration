= render :partial => "results"
- if policy(@competition).destroy_all_results?
  = link_to "Delete All", destroy_all_results_competition_path(@competition), method: :delete, class: "button small alert", data: { confirm: "This will delete ALL Trials Data for this competition. Are you sure?" }
%br
%h1 New Entered Trials Result
= render 'form'
