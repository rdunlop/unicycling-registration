%h1 List Directors
%table.sortable
  %thead
    %tr
      %th Event
      %th Director
  %tbody
    - @events.each do |event|
      %tr
        %td= link_to_if(can?(:show, event), event, event_path(event))
        %td
          - User.with_role(:director, event).each do |user|
            = user
