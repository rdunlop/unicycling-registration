- if user_signed_in? && @config.test_mode?
  .alert-box.alert.top_banner{ data: { alert: true } }
    %h2 Test Mode Enabled
    %span
      Current User Roles: #{current_user.roles.map{|role| role.name.humanize}.join(", ")}
    .span.js--hiddenToggleMenu
      Change Role
    .js--hiddenToggle
      - User.roles.each do |role|
        = link_to "Toggle #{role.to_s.humanize} Role", test_mode_role_event_configuration_path(role: role), :method => :post
        %br
    %a.close{ href: "#" } &times;
