- target_url = @config.event_url
%li
  - if target_url.nil?
    = link_to image_tag(@config.logo_file.url, :class => "logo"), root_url, {:class => "logo_link"}
  - else
    = link_to image_tag(@config.logo_file.url, :class => "logo"), target_url, {:class => "logo_link", :target => "_blank"}
