- content_for :content do
  %h1 #{@registrant.full_name}
  = wizard_progress_bar(@registrant.validated?)
  .row.show_hide_elements{ class: "show_#{@registrant.registrant_type}_elements" }
    = yield

= render template: "layouts/application"
