%h1 Convention Series - #{@series}

%ul
  %li= link_to "Add self to series", add_convention_series_path(@series), method: :post
  %li= link_to "Remove self from series", remove_convention_series_path(@series), method: :delete
