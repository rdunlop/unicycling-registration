= simple_form_for([:convention_setup, @page]) do |f|
  = render partial: "shared/error_messages", object: @page

  .row
    .small-6.columns= f.input :title
    .small-4.columns= f.input :slug
    .small-2.columns= f.input :position
  .row
    .small-4.columns
      = f.input :parent_page_id, collection: Page.ordinary.parent_or_single
    .small-4.columns.last= f.input :visible
  .row
    .small-12.columns
      = f.rich_text_area :body

  .row
    .small-12.columns
      = f.button :submit
