<h1>Manage Directors</h1>

<table>
    <caption>Directors</caption>
    <thead>
        <tr>
            <th>Event</th>
            <th>Director</th>
        </tr>
    </thead>
    <tbody>
        <% @events.each do |event| %>
            <tr>
                <td><%= link_to event, event_path(event) %></td>
                <td>
                  <% User.with_role(:director, event).each do |user| %>
                    <%= user %>
                  <% end %>
                </td>
            </tr>
        <% end %>
    </tbody>
</table>
