<%= form_for :copy_judges, :url=> copy_judges_event_category_judges_path(@event_category) do |f| -%>
  <%= f.select :event_id, Event.all.map{|e| [e.name, e.id]} -%>
  <%= f.submit 'Submit' %>
<% end %>
