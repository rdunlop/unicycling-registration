%h1= "#{@competition} - #{DistanceAttempt.model_name.human(count: 2)}"

#notice

= render partial: 'competitor_lookup'

.l--2up
  .l--column
    = render "form"
  #competitor_details_holder.l--column

.attempts_lists
  .recent_attempt_list
    = render partial: "attempt_list", locals: {title: "Recent Attempts", list: @recent_distance_attempts, display_delete: policy(@judge).can_judge? }
    (only the recent  ~20 are listed)

%hr
= render "/competitors/add_last_minute_competitor_form", competition: @competition
%hr

= link_to 'All Attempts', list_competition_distance_attempts_path(@competition)
%br
= link_to "Enter Tie Breakers", judge_tie_break_adjustments_path(@judge)
