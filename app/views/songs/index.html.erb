<h1>Listing <%= @registrant %>'s songs</h1>

<table>
  <thead>
    <tr>
      <th>Description</th>
      <th>File name</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @songs.each do |song| %>
      <tr>
        <td><%= song.description %></td>
        <td><%= song.event %></td>
        <td><%= song.human_name %></td>
        <td><%= link_to 'Download', song.song_file_name_url %></td>
        <td><%= link_to 'Edit', edit_song_path(song) %></td>
        <td><%= link_to 'Destroy', song, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<h1>New song</h1>

<%= render 'form' %>
