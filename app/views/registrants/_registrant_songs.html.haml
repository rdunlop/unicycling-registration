- cache_i18n [@config, @registrant], expires_in: 1.hour do
  - if @registrant.competitor && (@registrant.songs.count > 0 || !@config.music_submission_ended?)
    .registrant_data_group.non_printable
      %fieldset.data_block
        - cache_i18n ['songs_menu', @config, @registrant] do
          %legend= t(".header")
          %table
            %thead
              %tr
                %th= t(".event")
                %th= t(".song")
            %tbody
              - @registrant.songs.includes(:event).each do |song|
                %tr
                  %td= song.event
                  %td= song.description
        - if policy(Song.new).create?
          .non_printable
            = t(".instructions")
            = link_to t(".click_here"), registrant_songs_path(@registrant)
