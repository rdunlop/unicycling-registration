%h1 Import Data from CSV File

%h2 CSV Import Instructions

%p Please import a Comma-Separated-File (Windows CSV Format) file with the following format (for 'Points' Competitions):
.row
  .small-12.columns
    %ul
      %li Column 1: ID
      %li Column 2: Points
      %li Column 3: Details (to be printed on awards)

.row
  .small-12.columns
    %b Include NO header row

= form_tag(import_csv_competition_preliminary_external_results_path(@competition), {:method => :post, :multipart=>true}) do
  .row
    .small-6.columns
      = file_field_tag "file"
    .small-6.columns
      = submit_tag "Import CSV", class: "button small"

= render "results"

%hr

= link_to "Review Results", review_competition_preliminary_external_results_path(@competition), class: "button"
