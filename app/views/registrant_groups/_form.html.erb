<%= form_for(@registrant_group) do |f| %>
  <% if @registrant_group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@registrant_group.errors.count, "error") %> prohibited this registrant_group from being saved:</h2>

      <ul>
      <% @registrant_group.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :registrant_id %><br />
    <%= f.number_field :registrant_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
