%h1
  Trials Results - #{@competition}
= render :partial => "/competitions/results"
= render :partial => "/trials_results/results"

- if policy(@competition).export?
  = link_to 'Download Scores CSV', export_competition_path(@competition)
