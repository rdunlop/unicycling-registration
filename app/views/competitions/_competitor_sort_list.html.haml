%table.js--sortable{ data: { target: update_row_order_competitors_path } }
  %thead
    %tr
      %th Original Position
      %th Competitor
  %tbody#competitor-list
    - competitor_sort_list.competitors.order(:position).each do |f|
      %tr.item{ data: { item_id: f.id } }
        %td= f.position
        %td= f.to_s
