%h1 #{@expense_group} - Expense Group Details

%fieldset.form__fieldset
  = render 'form'

= link_to 'Delete', @expense_group, method: :delete, data: { confirm: 'Are you sure?' }
|
\#{link_to 'Back', expense_groups_path}
