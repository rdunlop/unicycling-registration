<h1>Listing expense_groups</h1>

<table class="alternate">
  <tr>
    <th>Group name</th>
    <th>Info Url</th>
    <th>Visible</th>
    <th>Position</th>
    <th>Competitor Free Options</th>
    <th>Non-Competitor Free Options</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @expense_groups.each do |expense_group| %>
  <tr>
    <td><%= expense_group.group_name %></td>
    <td><%= expense_group.info_url %></td>
    <td><%= expense_group.visible %></td>
    <td><%= expense_group.position %></td>
    <td><%= expense_group.competitor_free_options %></td>
    <td><%= expense_group.noncompetitor_free_options %></td>
    <td><%= link_to 'Show', expense_group %></td>
    <td><%= link_to 'Edit', edit_expense_group_path(expense_group) %></td>
    <td><%= link_to 'Destroy', expense_group, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<h2>New expense_group</h2>

<%= render 'form' %>
