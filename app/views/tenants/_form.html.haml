= simple_form_for(@new_tenant) do |f|
  = render partial: "shared/error_messages", object: @new_tenant

  %table
    %tbody
      %tr
        %th= f.label :subdomain
        %td= f.text_field :subdomain, placeholder: 'naucc2000'
        %td What is the prefix you want for the domain? (&lt;subdomain&gt;.#{Rails.application.secrets.domain})
      %tr
        %th= f.label :description
        %td= f.text_field :description, placeholder: "NAUCC 2000"
        %td Description to be used on this page. (you can customize the name later)
      %tr
        %th= f.label :admin_upgrade_code
        %td= f.text_field :admin_upgrade_code
        %td Specify the code you want to use to upgrade your user account to an Convention Admin account from the "My Account" page.
      %tr
        %td.creation_code_field= label_tag :code, "Creation Access Code"
        %td.creation_code_field= password_field_tag :code
        %td.creation_code_field
          In order to create a convention, you must prove you are allowed to do so by entering the Convention Creation Access code.
  %br

  = f.button :submit, "Create New Convention"
