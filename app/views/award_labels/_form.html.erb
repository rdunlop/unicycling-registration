<%= form_for(@award_label.new_record? ? [@user, @award_label] : [@award_label]) do |f| %>
  <% if @award_label.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@award_label.errors.count, "error") %> prohibited this award_label from being saved:</h2>

      <ul>
      <% @award_label.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <table>
    <thead>
      <tr>
        <th><%= f.label :bib_number %></th>
        <th><%= f.label :first_name %></th>
        <th><%= f.label :last_name %></th>
        <th><%= f.label :partner_first_name %></th>
        <th><%= f.label :partner_last_name %></th>
        <th><%= f.label :competition_name %></th>
        <th><%= f.label :team_name %></th>
        <th><%= f.label :age_group %></th>
        <th><%= f.label :gender %></th>
        <th><%= f.label :details %></th>
        <th><%= f.label :place %></th>
        <th><%= f.label :registrant_id %></th>
        <th></th>
      </tr>
    </thead>
    <tbody>
      <td><%= f.number_field :bib_number %></td>
      <td><%= f.text_field :first_name %></td>
      <td><%= f.text_field :last_name %></td>
      <td><%= f.text_field :partner_first_name %></td>
      <td><%= f.text_field :partner_last_name %></td>
      <td><%= f.text_field :competition_name %></td>
      <td><%= f.text_field :team_name %></td>
      <td><%= f.text_field :age_group %></td>
      <td><%= f.text_field :gender %></td>
      <td><%= f.text_field :details %></td>
      <td><%= f.number_field :place %></td>
      <td><%= f.select :registrant_id, Registrant.all.map {|reg| [reg, reg.id] } %></td>
      <td><%= f.submit %></td>
    </tbody>
  </table>
  </div>
<% end %>
