
%h1 Generic Volunteers Report

%table.volunteers.sortable
  %thead
    %tr
      %th ID
      %th First Name
      %th Last Name
      %th Age
      %th Country
      %th Email

  %tbody
    - @volunteers.includes(:contact_detail, :user).each do |reg|
      %tr
        %td= reg.bib_number
        %td= reg.first_name
        %td= reg.last_name
        %td= reg.age
        %td= reg.contact_detail.country
        %td= reg.email
