---
fr:
  activerecord:
    attributes:
      event:
        accepts_wheel_size_override: Accepte le changement de taille de roue
        artistic: Artistique
        best_time_format: Format du mailleur temps
        id: ID
        standard_skill: Standard Skill (artistique technique)
        visible: Visible
    models:
      event:
        one: Événement
        other: Événements
