test:
  pre:
    - cp config/secrets.yml.template config/secrets.yml:
        parallel: true
  post:
    - bundle exec script/circle.rb

general:
  artifacts:
    - coverage/
    - coverage/.resultset.json
